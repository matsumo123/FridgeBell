<%= link_to favorite_path(current_user.favorites.find_by(food_id: food.id)),
  id: "unfavorite-button-for-food-#{food.id}",
  data: { turbo_method: :delete } do %>
  <%= heroicon "heart", options: { class: "w-5 h-5 text-rose-500" } %>
<% end %>
